# Структура проекта web-app

## 📋 Содержание

1. [Дерево проекта](#-дерево-проекта)
2. [Описание файлов и директорий](#-описание-файлов-и-директорий)
3. [Design System](#-design-system-skeleton)
4. [Поток данных](#-поток-данных)
5. [Технологический стек](#️-технологический-стек)
6. [Команды разработки](#-команды-разработки)

---

## 📁 Дерево проекта

```
web-app/
│
├── 📄 Конфигурационные файлы
│   ├── package.json              # Зависимости и скрипты npm
│   ├── package-lock.json         # Зафиксированные версии зависимостей
│   ├── tsconfig.json             # Конфигурация TypeScript
│   ├── jsconfig.json             # Конфигурация JavaScript (для IDE)
│   ├── tailwind.config.js        # Конфигурация Tailwind CSS + Skeleton
│   ├── postcss.config.js         # Конфигурация PostCSS
│   ├── rollup.config.js          # Конфигурация сборщика Rollup
│   ├── README.md                 # Стандартный README Svelte
│   └── HANDLERS.md               # Документация API handlers
│
├── 📁 scripts/
│   └── setupTypeScript.js        # Скрипт настройки TypeScript
│
├── 📁 public/                    # Статические файлы (публикуются как есть)
│   ├── index.html                # Главный HTML файл
│   ├── global.css                # Глобальные стили
│   ├── app.css                   # Основной CSS с Tailwind и Skeleton
│   ├── favicon.png               # Иконка сайта
│   └── 📁 build/                 # Собранные файлы (генерируются)
│       ├── bundle.js             # Собранный JavaScript
│       ├── bundle.css             # Собранный CSS
│       ├── bundle.js.map          # Source map для JS
│       └── app.css                # Обработанный CSS
│
└── 📁 src/                       # Исходный код приложения
    │
    ├── 📄 main.js                 # Точка входа приложения
    ├── 📄 App.svelte              # Главный компонент (роутинг)
    │
    ├── 📁 lib/                    # Библиотеки и утилиты
    │   ├── api.ts                 # API клиент (с моками)
    │   ├── types.ts               # TypeScript типы
    │   ├── mocks.ts               # Моки данных для разработки
    │   ├── themes.ts              # Конфигурация тем Skeleton
    │   │
    │   ├── 📁 stores/             # Svelte stores (глобальное состояние)
    │   │   └── user.ts            # Store текущего пользователя
    │   │
    │   └── 📁 components/         # Переиспользуемые компоненты
    │       ├── Chart.svelte       # Компонент графиков статистики
    │       ├── Modal.svelte       # Модальное окно
    │       ├── GameForms.svelte    # Форма создания/редактирования игры
    │       ├── GameTable.svelte    # Таблица игр (не используется)
    │       ├── Table.svelte        # Таблица (не используется)
    │       └── UserCard.svelte     # Карточка пользователя (не используется)
    │
    └── 📁 routes/                 # Страницы приложения
        ├── layout.svelte         # Общий layout (навигация)
        ├── layout.ts              # Load функция для layout (не используется)
        │
        ├── page.svelte            # Главная страница (Leaderboard)
        ├── page.ts                # Load функция для главной страницы
        │
        ├── 📁 login/
        │   └── page.svelte        # Страница входа/регистрации
        │
        └── 📁 users/
            └── 📁 [id]/           # Динамический роут для пользователя
                ├── page.svelte     # Страница профиля пользователя
                └── page.ts         # Load функция для страницы пользователя
```

---

## 🎯 Описание файлов и директорий

### 📦 Корневые конфигурационные файлы

#### `package.json`
**Назначение:** Управление зависимостями и скриптами проекта

**Основные зависимости:**
- **Svelte** (`^3.55.0`) - фреймворк для UI
- **@skeletonlabs/skeleton** (`^4.11.0`) - Design system компоненты
- **@skeletonlabs/tw-plugin** (`^0.4.1`) - Плагин Tailwind для Skeleton
- **Tailwind CSS** (`^4.1.18`) - Utility-first CSS фреймворк
- **TypeScript** (`^5.9.3`) - Типизация

**Скрипты:**
- `npm run dev` - Запуск в режиме разработки с hot-reload
- `npm run build` - Сборка для продакшена
- `npm run start` - Запуск собранного приложения (порт 8080)

#### `rollup.config.js`
**Назначение:** Конфигурация сборщика Rollup

**Основные плагины:**
- `rollup-plugin-svelte` - Компиляция Svelte компонентов
- `@rollup/plugin-typescript` - Обработка TypeScript
- `rollup-plugin-postcss` - Обработка CSS через PostCSS
- `rollup-plugin-css-only` - Извлечение CSS из компонентов
- `rollup-plugin-livereload` - Автоперезагрузка в dev режиме

**Особенности:**
- Использует `svelte-preprocess` для TypeScript и PostCSS
- Настроен alias `$lib` для импортов из `src/lib`
- Генерирует source maps для отладки

#### `tailwind.config.js`
**Назначение:** Конфигурация Tailwind CSS и Skeleton

**Ключевые настройки:**
- Использует тему `wintry` из Skeleton
- Подключены плагины `@tailwindcss/forms` и `@tailwindcss/typography`
- Настроен dark mode через `class`
- Включены все файлы Skeleton в content для правильной обработки

#### `tsconfig.json`
**Назначение:** Конфигурация TypeScript компилятора

**Настройки:**
- `module: "ESNext"` - ES модули
- `target: "ES2020"` - Целевая версия ES
- `baseUrl: "."` с `paths: { "$lib/*": ["./src/lib/*"] }` - Алиасы путей
- `noEmit: true` - TypeScript только для проверки типов (компиляция через Rollup)

---

### 📁 `public/` - Статические файлы

#### `index.html`
**Назначение:** Главный HTML файл приложения

**Содержимое:**
- Базовая структура HTML5
- Атрибут `data-theme="wintry"` для Skeleton темы
- Подключение CSS и JS бандлов
- Мета-теги для viewport и charset

#### `app.css`
**Назначение:** Основной файл стилей

**Импорты:**
```css
@import '@skeletonlabs/skeleton/themes/theme-wintry.css';
@import '@skeletonlabs/skeleton/styles/skeleton.css';
@tailwind base;
@tailwind components;
@tailwind utilities;
```

**Слои:**
- `@layer base` - Базовые стили (smooth scroll, antialiasing)

#### `global.css`
**Назначение:** Глобальные стили (минимальные, т.к. используется Tailwind)

---

### 📁 `src/` - Исходный код

#### `main.js`
**Назначение:** Точка входа приложения

**Функционал:**
- Импортирует `App.svelte`
- Импортирует CSS (`app.css`)
- Инициализирует тему Skeleton (`data-theme="wintry"`)
- Создает экземпляр Svelte приложения и монтирует в `document.body`

#### `App.svelte`
**Назначение:** Главный компонент с кастомным роутингом

**Архитектура:**
- **Роутинг:** Кастомная реализация через `window.location.pathname` и `history.pushState`
- **Состояние:**
  - `currentPath` - текущий путь
  - `homeData` - кэшированные данные главной страницы
  - `userData` - кэшированные данные пользователей (по ID)
  - `currentComponent` - текущий компонент страницы
  - `loading` - состояние загрузки
  - `error` - ошибки

**Маршруты:**
- `/` → `Home` (page.svelte) - Leaderboard
- `/login` → `Login` (login/page.svelte) - Авторизация
- `/users/[id]` → `UserPage` (users/[id]/page.svelte) - Профиль

**Функции загрузки данных:**
- `loadHomeData()` - Загружает leaderboard, players, и текущие игры
- `loadUserData(id)` - Загружает данные пользователя и его игры

**Обработка событий:**
- `handleNavigation()` - Обработка изменения пути
- `handleClick()` - Перехват кликов по ссылкам для SPA навигации
- `popstate` listener - Обработка кнопок назад/вперед

---

### 📁 `src/lib/` - Библиотеки и утилиты

#### `api.ts`
**Назначение:** API клиент с поддержкой моков

**Архитектура:**
- **Режимы работы:**
  - `USE_MOCKS = true` - Использование моков (по умолчанию)
  - Автоматическое переключение на моки если backend недоступен

**Функции:**
- `api<T>(url, options)` - Базовая функция для запросов
- `login(username, password)` - Авторизация
- `register(username, password)` - Регистрация
- `getPlayers()` - Список всех игроков
- `getPlayer(id)` - Данные игрока
- `updatePlayer(id, data)` - Обновление профиля
- `getLeaderboard()` - Таблица лидеров
- `getPlayerGames(playerId)` - Игры игрока
- `updateGamePlayed(...)` - Обновление записи об игре
- `getGames()` - Список всех игр
- `getGame(id)` - Данные игры

**Особенности:**
- Автоматическое добавление JWT токена из localStorage
- Обработка ошибок с понятными сообщениями
- Имитация задержки сети в моках (100ms)

#### `types.ts`
**Назначение:** TypeScript типы для всего приложения

**Типы:**
- `Player` - Пользователь (ID, Username, Description, Img, Email)
- `LeaderboardRow` - Строка лидерборда (Player_id, total, comleted, drop, reroll, current_game)
- `Game` - Игра (ID, Points, HoursToBeat, Title, URL, CreatedAt)
- `GamePlayed` - Запись об игре пользователя (Player_id, game_id, status, scores, dates, comment, rating, time_played)
- `AuthResponse` - Ответ авторизации (token, player)

#### `mocks.ts`
**Назначение:** Моки данных для разработки без backend

**Содержимое:**
- `mockPlayers` - Массив из 3 тестовых игроков
- `mockGames` - Массив из 3 тестовых игр
- `mockGamesPlayed` - Массив записей об играх
- `mockLeaderboard` - Данные лидерборда

**Функции:**
- `mockLogin(username, password)` - Имитация входа
- `mockRegister(username, password)` - Имитация регистрации
- `getMockPlayers()` - Получить всех игроков
- `getMockPlayer(id)` - Получить игрока по ID
- `getMockPlayerGames(playerId)` - Получить игры игрока
- `getMockGames()` - Получить все игры
- `getMockGame(id)` - Получить игру по ID
- `getMockLeaderboard()` - Получить лидерборд

#### `themes.ts`
**Назначение:** Конфигурация тем Skeleton (заглушка, темы настраиваются в tailwind.config.js)

#### `stores/user.ts`
**Назначение:** Svelte store для текущего пользователя

**Функционал:**
- `writable<Player | null>` - Реактивное хранилище пользователя
- Используется для глобального доступа к данным текущего пользователя
- Обновляется при входе/выходе

---

### 📁 `src/lib/components/` - Переиспользуемые компоненты

#### `Chart.svelte`
**Назначение:** Компонент графиков статистики

**Props:**
- `data: LeaderboardRow[]` - Данные лидерборда
- `players: Player[]` - Список игроков

**Функционал:**
- Переключение периода: Неделя / Месяц
- Переключение типа данных: Поинты / Пройдено / Дроп / Реролл
- Визуализация в виде столбчатой диаграммы
- Использует Skeleton компоненты для кнопок

**Визуализация:**
- Столбчатая диаграмма с высотой пропорциональной значению
- Цвета зависят от типа данных (warning/success/error)
- Показывает топ-10 игроков

#### `Modal.svelte`
**Назначение:** Универсальное модальное окно

**Props:**
- `isOpen: boolean` - Видимость модального окна
- `title: string` - Заголовок (опционально)

**Функционал:**
- Backdrop с blur эффектом
- Закрытие по клику вне окна
- Закрытие по Escape
- Кнопка закрытия (✕)
- Использует Skeleton `card` для стилизации

#### `GameForms.svelte`
**Назначение:** Форма создания/редактирования записи об игре

**Props:**
- `playerId: string` - ID игрока
- `isOpen: boolean` - Видимость формы

**Функционал:**
- Поиск игры по названию (debounce 300ms)
- Выбор игры из списка
- Выбор статуса: В процессе / Пройдено / Дроп / Реролл
- Поля: время в игре, комментарий, оценка
- Автоматическая установка текущей даты
- Использует `Modal.svelte` для отображения

**Особенности:**
- Автопоиск при вводе (минимум 2 символа)
- Показывает список найденных игр
- Валидация выбранной игры перед отправкой

#### `GameTable.svelte`, `Table.svelte`, `UserCard.svelte`
**Назначение:** Неиспользуемые компоненты (оставлены для возможного будущего использования)

---

### 📁 `src/routes/` - Страницы приложения

#### `layout.svelte`
**Назначение:** Общий layout для всех страниц

**Содержимое:**
- Навигационная панель (navbar)
- Ссылка на главную (GameTracker)
- Кнопка входа / Имя пользователя + Выход
- Слот для контента страниц

**Функционал:**
- Загрузка пользователя из localStorage при монтировании
- Функция `logout()` - очистка состояния и перенаправление

**Стилизация:**
- Использует Skeleton компоненты (`btn variant-ghost-*`)
- Sticky navbar с backdrop blur
- Адаптивный контейнер

#### `page.svelte` (Главная страница)
**Назначение:** Страница Leaderboard (таблица лидеров)

**Props:**
- `data: { leaderboard: LeaderboardRow[], players: Player[] }`

**Содержимое:**
- Заголовок "🏆 Leaderboard"
- Компонент `Chart` для графиков
- Таблица лидеров с колонками:
  - Место (с медалями 🥇🥈🥉)
  - Пользователь (ссылка на профиль)
  - Текущая игра
  - Поинты (badge warning)
  - Пройдено (badge success)
  - Дроп (badge error)
  - Реролл (badge warning)

**Особенности:**
- Подсветка первого места (bg-primary-500/10)
- Hover эффекты на строках
- Обработка пустого состояния

#### `page.ts` (Load функция для главной)
**Назначение:** Загрузка данных для главной страницы

**Функционал:**
- Вызывает `loadHomeData()` из `App.svelte`
- Возвращает данные для компонента

#### `login/page.svelte`
**Назначение:** Страница входа и регистрации

**Функционал:**
- Переключение между режимами входа и регистрации
- Поля: username, password
- Валидация полей
- Обработка ошибок
- Сохранение токена в localStorage
- Перенаправление на профиль после успешного входа

**Стилизация:**
- Использует Skeleton компоненты (`card`, `btn`, `input`, `label`)
- Центрированная форма
- Обработка состояния загрузки

#### `users/[id]/page.svelte`
**Назначение:** Страница профиля пользователя

**Props:**
- `data: { player: Player, games: GamePlayed[], isOwner: boolean }`

**Содержимое:**
- **Профиль:**
  - Аватар
  - Имя пользователя
  - Описание
  - Статистика (игр, очки, пройдено, дроп, реролл)
  - Кнопка редактирования (только для владельца)

- **Форма создания записи:**
  - Кнопка "Создать запись" (только для владельца)
  - Компонент `GameForms`

- **Модальное окно редактирования профиля:**
  - Поля: Username, Description, Img URL
  - Сохранение через API

- **Таблица игр:**
  - Колонки: Дата, Время, Игра, Статус, Очки, Комментарий, Оценка
  - Сортировка по дате (новые сверху)
  - Ссылки на игры (если есть URL)
  - Badges для статусов

**Особенности:**
- Определение владельца страницы (`isOwner`)
- Условное отображение элементов редактирования
- Обработка пустого состояния

#### `users/[id]/page.ts`
**Назначение:** Load функция для страницы пользователя

**Функционал:**
- Загружает данные игрока
- Загружает игры игрока
- Обогащает игры названиями и URL
- Определяет является ли текущий пользователь владельцем
- Возвращает данные для компонента

---

## 🎨 Design System (Skeleton)

### Используемые компоненты Skeleton:

1. **Buttons:**
   - `btn variant-filled-primary` - Основная кнопка
   - `btn variant-ghost-primary` - Прозрачная кнопка
   - `btn variant-ghost-surface` - Прозрачная кнопка (surface)
   - `btn variant-filled-warning/success/error` - Цветные кнопки
   - `btn variant-soft-*` - Мягкие варианты

2. **Cards:**
   - `card` - Базовый компонент карточки

3. **Badges:**
   - `badge variant-soft-warning` - Предупреждение
   - `badge variant-soft-success` - Успех
   - `badge variant-soft-error` - Ошибка
   - `badge variant-soft-primary` - Основной

4. **Forms:**
   - `input` - Поле ввода
   - `label` с `label-text` - Метка поля

5. **Colors:**
   - `text-primary-400` - Основной цвет текста
   - `text-surface-500` - Вторичный текст
   - `bg-surface-900` - Фон
   - `bg-surface-800` - Фон элементов
   - `border-surface-700` - Границы

### Тема: `wintry`
- Темная тема по умолчанию
- Сине-голубая цветовая схема
- Современный минималистичный дизайн

---

## 🔄 Поток данных

### Инициализация приложения:
1. `main.js` → Инициализация темы Skeleton
2. `main.js` → Создание экземпляра `App.svelte`
3. `App.svelte` → Загрузка начального маршрута
4. `App.svelte` → Вызов `loadHomeData()` или `loadUserData()`
5. `api.ts` → Запрос к API (или мокам)
6. Данные → Передача в компонент страницы

### Авторизация:
1. `login/page.svelte` → Ввод данных
2. `api.ts` → `login()` или `register()`
3. Токен → Сохранение в `localStorage`
4. Пользователь → Сохранение в `stores/user.ts`
5. Перенаправление → `/users/[id]`

### Навигация:
1. Клик по ссылке → `handleClick()` в `App.svelte`
2. `history.pushState()` → Изменение URL
3. `handleNavigation()` → Определение маршрута
4. Загрузка данных → Вызов соответствующей функции
5. Рендеринг → `<svelte:component>`

---

## 🛠️ Технологический стек

- **Frontend Framework:** Svelte 3
- **Language:** TypeScript
- **Styling:** Tailwind CSS 4 + Skeleton UI
- **Build Tool:** Rollup
- **Preprocessing:** svelte-preprocess (TypeScript + PostCSS)
- **State Management:** Svelte Stores
- **Routing:** Custom SPA routing
- **API:** Fetch API с поддержкой моков

---

## 📝 Заметки

### Неиспользуемые компоненты:
- `GameTable.svelte` - Возможно использовался ранее
- `Table.svelte` - Общий компонент таблицы
- `UserCard.svelte` - Карточка пользователя

### Возможные улучшения:
- Добавить обработку ошибок 404
- Реализовать lazy loading для компонентов
- Добавить анимации переходов между страницами
- Реализовать кэширование данных с TTL
- Добавить индикаторы загрузки для отдельных элементов

### Конфигурация для продакшена:
- Установить `USE_MOCKS = false` в `api.ts`
- Настроить правильный `API` URL
- Проверить CORS настройки backend
- Оптимизировать bundle size

---

## 🚀 Команды разработки

```bash
# Установка зависимостей
npm install

# Запуск в режиме разработки
npm run dev

# Сборка для продакшена
npm run build

# Запуск собранного приложения
npm run start
```

---

*Документация создана: 2025-01-23*
*Версия проекта: 1.0.0*

