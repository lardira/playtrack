# Техническое задание: GameTracker Web Application

## 📋 Содержание

1. [Общее описание](#1-общее-описание)
2. [Цели и задачи проекта](#2-цели-и-задачи-проекта)
3. [Функциональные требования](#3-функциональные-требования)
4. [Технические требования](#4-технические-требования)
5. [Требования к дизайну](#5-требования-к-дизайну)
6. [API требования](#6-api-требования)
7. [Структура страниц](#7-структура-страниц)
8. [Компоненты](#8-компоненты)
9. [Обработка ошибок](#9-обработка-ошибок)
10. [Критерии приемки](#10-критерии-приемки)

---

## 1. Общее описание

### 1.1 Название проекта
**PlayTracker** - веб-приложение для отслеживания игрового прогресса пользователей.

### 1.2 Назначение
Приложение позволяет пользователям:
- Отслеживать свой игровой прогресс
- Вести учет пройденных игр
- Сравнивать свои достижения с другими игроками
- Просматривать рейтинг игроков по очкам и статистике

### 1.3 Целевая аудитория
- Геймеры, желающие отслеживать свой игровой прогресс
- Пользователи, которые хотят делиться своими достижениями
- Игроки, интересующиеся статистикой других участников

---

## 2. Цели и задачи проекта

### 2.1 Основные цели
1. Создать удобный интерфейс для отслеживания игр
2. Реализовать систему рейтинга игроков
3. Обеспечить возможность просмотра и редактирования профиля
4. Предоставить визуализацию статистики

### 2.2 Задачи
- Разработать SSR на Svelte
- Интегрировать с backend API (Go)
- Реализовать систему авторизации
- Создать адаптивный дизайн с использованием Skeleton UI
- Обеспечить работу с моками для разработки

---

## 3. Функциональные требования

### 3.1 Авторизация и регистрация

#### 3.1.1 Страница входа/регистрации (`/login`)
**Требования:**
- Единая страница для входа и регистрации
- Переключение между режимами через кнопку
- Поля формы:
  - **Username** (обязательное, текстовое поле)
  - **Password** (обязательное, поле пароля)
- Валидация:
  - Проверка заполненности всех обязательных полей
  - Отображение ошибок валидации
- Поведение:
  - При успешной авторизации → перенаправление на `/users/{id}`
  - При успешной регистрации → перенаправление на `/users/{id}`
  - Сохранение JWT токена в `localStorage`
  - Сохранение данных пользователя в `localStorage` и store
- Состояния:
  - Индикатор загрузки при отправке формы
  - Блокировка формы во время загрузки
  - Отображение ошибок от сервера

#### 3.1.2 Выход из системы
- Кнопка "Выйти" в навигации (только для авторизованных)
- Очистка `localStorage` (token, user)
- Сброс store пользователя
- Перенаправление на главную страницу

### 3.2 Главная страница - Leaderboard (`/`)

#### 3.2.1 Заголовок
- Название: "🏆 Leaderboard"
- Подзаголовок: "Рейтинг игроков по очкам и достижениям"

#### 3.2.2 Компонент графиков статистики
**Требования:**
- Переключение периода:
  - Кнопка "Неделя" (активна по умолчанию)
  - Кнопка "Месяц"
- Переключение типа данных:
  - Кнопка "Поинты" (активна по умолчанию)
  - Кнопка "Пройдено"
  - Кнопка "Дроп"
  - Кнопка "Реролл"
- Визуализация:
  - Линейная диаграмма для отслеживания корреляциии очки - день 
  - Показ всех игроков, где для каждого свой цвет
- Информация:
  - Текст: "Период: {Неделя/Месяц} | Показано: {Тип данных}"

#### 3.2.3 Таблица лидеров
**Колонки:**
1. **Место**
   - Номер места (#1, #2, #3...)
   - Иконки медалей для топ-3:
     - 🥇 для 1 места
     - 🥈 для 2 места
     - 🥉 для 3 места
   - Подсветка первого места (bg-primary-500/10)

2. **Пользователь**
   - Никнейм (ссылка на профиль)
   - Hover эффект с иконкой стрелки
   - Цвет: у каждого пользователя свой цвет, который задан игрок на backend части

3. **Текущая игра**
   - Название игры, которую проходит игрок
   - Если нет текущей игры → "Перерыв"
   - Определяется как последняя игра со статусом "В процессе"

4. **Поинты**
   - Общее количество очков
   - Badge: variant-soft-warning

5. **Пройдено**
   - Количество пройденных игр
   - Badge: variant-soft-success

6. **Дроп**
   - Количество дропнутых игр
   - Badge: variant-soft-error

7. **Реролл**
   - Количество рероллов
   - Badge: variant-soft-warning

**Дополнительно:**
- Пустое состояние при отсутствии данных
- Сортировка по очкам (от большего к меньшему). Место выдается в соответствии с топом по очкам.

### 3.3 Страница профиля пользователя (`/users/{id}`)

#### 3.3.1 Блок профиля
**Элементы:**
- **Аватар**
  - Изображение из поля `Img`
  - Fallback: `/favicon.png`
  - Размер: 128x128px
  - Скругление: rounded-xl
  - Border: border-surface-600

- **Информация о пользователе:**
  - Никнейм (h2, text-3xl, text-primary-400)
  - Описание (или "Нет описания")
  - Кнопка "✏️ Редактировать" (только для владельца)

- **Статистика:**
  - 🎮 Игр: общее количество игр
  - 🏆 Очки: сумма всех очков (text-warning-400)
  - ✅ Пройдено: количество со статусом "Пройдено" (badge success)
  - ❌ Дроп: количество со статусом "Дроп" (badge error)
  - 🔄 Реролл: количество со статусом "Реролл" (badge warning)

#### 3.3.2 Создание записи об игре (только для владельца)
**Требования:**
- Кнопка "➕ Создать запись"
- При клике открывается модальное окно с формой
- Поля формы:
  - **Игра** (обязательное)
    - Поиск по названию (минимум 2 символа)
    - Debounce: 300ms
    - Выпадающий список найденных игр
    - Если игра не найдена, то создается в базе как Game
    - Отображение выбранной игры
  - **Статус** (обязательное, select)
    - Варианты: "В процессе", "Пройдено", "Дроп", "Реролл"
    - По умолчанию: "В процессе"
  - **Время в игре** (текстовое поле)
    - Placeholder: "Например: 15ч 30м 0с"
  - **Комментарий** (textarea, 4 строки)
    - Placeholder: "Ваши впечатления об игре..."
  - **Оценка** (текстовое поле)
    - Placeholder: "Например: 8/100"
- Автоматическая установка:
  - `start_date`: текущая дата
- Валидация:
  - Обязательное поле: Игра
  - Проверка выбора игры перед отправкой
- Поведение:
  - После успешного создания → перезагрузка страницы
  - Закрытие модального окна
  - Сброс формы

#### 3.3.3 Редактирование профиля (только для владельца)
**Требования:**
- Модальное окно с формой
- Поля:
  - **Никнейм** (обязательное)
  - **Описание** (textarea, 4 строки)
  - **URL аватарки** (текстовое поле)
    - Placeholder: "https://..."
- Поведение:
  - Сохранение через API
  - Обновление данных на странице
  - Закрытие модального окна
  - Перезагрузка страницы после сохранения
- Состояния:
  - Индикатор "Сохранение..." при отправке
  - Блокировка формы
  - Отображение ошибок

#### 3.3.4 Таблица игр
**Колонки:**
1. **Дата**
   - Формат: `start_date` из данных
   - Если отсутствует → "-"

2. **Время**
   - Поле `time_played`
   - Если отсутствует → "-"

3. **Игра**
   - Название игры (из `gameTitle`)
   - Если есть `gameURL` → ссылка (target="_blank", rel="noopener noreferrer")
   - Цвет: primary-400, hover: primary-300
   - Если нет URL → обычный текст

4. **Статус**
   - Badge с цветом в зависимости от статуса:
     - "Пройдено" → variant-soft-success
     - "Дроп" → variant-soft-error
     - "Реролл" → variant-soft-warning
     - "В процессе" → variant-soft-primary

5. **Очки**
   - Значение из `scores`
   - Badge: variant-soft-warning
   - Если 0 → "0"

6. **Комментарий**
   - Текст комментария
   - Если отсутствует → "-"
   - Максимальная ширина с truncate
   - Tooltip при наведении (полный текст)

7. **Оценка**
   - Значение из `rating`
   - Если отсутствует → "-"

**Дополнительно:**
- Сортировка по дате (новые сверху)
- Hover эффект на строках
- Пустое состояние при отсутствии игр

### 3.4 Навигация

#### 3.4.1 Навигационная панель
**Элементы:**
- **Логотип/Название**
  - Текст: "🎮 GameTracker"
  - Ссылка на главную (`/`)
  - Стиль: text-2xl, font-bold, text-primary-400

- **Правая часть:**
  - Для неавторизованных:
    - Кнопка "Войти" (ссылка на `/login`)
  - Для авторизованных:
    - Ссылка с никнеймом (на `/users/{id}`)
    - Кнопка "Выйти"

**Стилизация:**
- Sticky позиционирование (top-0, z-50)
- Backdrop blur эффект
- Border-bottom: border-surface-700
- Фон: bg-surface-900

### 3.5 Роутинг

#### 3.5.1 Маршруты
- `/` → Главная страница (Leaderboard)
- `/login` → Страница входа/регистрации
- `/users/{id}` → Страница профиля пользователя
- Любой другой путь → редирект на `/`

#### 3.5.2 Реализация
- Использование `window.location.pathname` и `history.pushState`
- Обработка кнопок назад/вперед (`popstate`)
- Перехват кликов по ссылкам для SPA навигации

#### 3.5.3 Загрузка данных
- Кэширование данных страниц
- Индикатор загрузки при переходе
- Обработка ошибок загрузки

---

## 4. Технические требования

### 4.1 Технологический стек

#### 4.1.1 Frontend Framework
- **Svelte 3.55.0+**
  - Компонентный подход
  - Реактивность
  - Компиляция в ванильный JavaScript

#### 4.1.2 Язык программирования
- **TypeScript 5.9.3+**
  - Строгая типизация
  - Проверка типов на этапе разработки
  - Поддержка ES2020

#### 4.1.3 Стилизация
- **Tailwind CSS 4.1.18+**
  - Utility-first подход
  - Кастомная конфигурация
- **Skeleton UI 4.11.0+**
  - Design system компоненты
  - Тема: `wintry`
  - Компоненты: buttons, cards, badges, inputs, modals

#### 4.1.4 Сборка
- **Rollup 3.15.0+**
  - Бандлинг JavaScript
  - Обработка Svelte компонентов
  - Обработка TypeScript
  - Обработка CSS через PostCSS
  - Source maps для отладки

#### 4.1.5 Дополнительные инструменты
- **svelte-preprocess** - препроцессинг (TypeScript, PostCSS)
- **PostCSS** - обработка CSS
- **autoprefixer** - автопрефиксы для CSS

### 4.2 Структура проекта

```
web-app/
├── src/
│   ├── main.js              # Точка входа
│   ├── App.svelte            # Главный компонент (роутинг)
│   ├── lib/
│   │   ├── api.ts           # API клиент
│   │   ├── types.ts         # TypeScript типы
│   │   ├── mocks.ts         # Моки данных
│   │   ├── themes.ts        # Конфигурация тем
│   │   ├── stores/
│   │   │   └── user.ts      # Store пользователя
│   │   └── components/
│   │       ├── Chart.svelte
│   │       ├── Modal.svelte
│   │       └── GameForms.svelte
│   └── routes/
│       ├── layout.svelte
│       ├── page.svelte
│       ├── page.ts
│       ├── login/
│       │   └── page.svelte
│       └── users/
│           └── [id]/
│               ├── page.svelte
│               └── page.ts
├── public/
│   ├── index.html
│   ├── app.css
│   └── global.css
└── Конфигурационные файлы
```

### 4.3 State Management

#### 4.3.1 Svelte Stores
- **user store** (`lib/stores/user.ts`)
  - Тип: `writable<Player | null>`
  - Хранение текущего пользователя
  - Автоматическая синхронизация с localStorage
  - Загрузка при инициализации

#### 4.3.2 Локальное состояние
- Состояние компонентов через переменные
- Props для передачи данных между компонентами
- Reactive statements (`$:`) для вычисляемых значений

### 4.4 API Integration

#### 4.4.1 API Client (`lib/api.ts`)
- Базовая функция `api<T>(url, options)`
- Автоматическое добавление JWT токена
- Обработка ошибок
- Поддержка моков для разработки

#### 4.4.2 Режимы работы
- **Режим моков** (USE_MOCKS = true)
  - Использование данных из `mocks.ts`
  - Имитация задержки сети (100ms)
- **Режим реального API** (USE_MOCKS = false)
  - Запросы к `http://localhost:8080`
  - Автоматическое переключение на моки при недоступности backend

#### 4.4.3 Endpoints
См. раздел [6. API требования](#6-api-требования)

### 4.5 Обработка данных

#### 4.5.1 Кэширование
- Кэширование данных главной страницы
- Кэширование данных пользователей (по ID)
- Перезагрузка при необходимости

#### 4.5.2 Загрузка данных
- Асинхронная загрузка через функции `load*`
- Индикаторы загрузки
- Обработка ошибок с fallback значениями

### 4.6 Браузерная совместимость
- Современные браузеры (Chrome, Firefox, Safari, Edge)
- Поддержка ES2020
- Responsive дизайн (mobile-first)

---

## 5. Требования к дизайну

### 5.1 Design System

#### 5.1.1 Skeleton UI
- **Тема:** `wintry`
- **Режим:** Dark mode (по умолчанию)
- **Цветовая схема:**
  - Primary: сине-голубые оттенки
  - Success: зеленый
  - Warning: желтый
  - Error: красный
  - Surface: серые оттенки

#### 5.1.2 Компоненты Skeleton
- `btn` - кнопки (variant-filled-primary, variant-ghost-*, variant-soft-*)
- `card` - карточки
- `badge` - бейджи (variant-soft-*)
- `input` - поля ввода
- `label` с `label-text` - метки полей

### 5.2 Типографика
- Заголовки: font-bold
- Основной текст: стандартный размер
- Вторичный текст: text-surface-500
- Размеры:
  - h1: text-4xl
  - h2: text-3xl / text-2xl
  - h3: text-2xl

### 5.3 Цвета
- **Primary:**
  - Основной: text-primary-400
  - Hover: text-primary-300
- **Surface:**
  - Фон: bg-surface-900
  - Элементы: bg-surface-800
  - Границы: border-surface-700
  - Текст: text-surface-400, text-surface-500

### 5.4 Spacing
- Использование Tailwind spacing scale
- Контейнеры: container mx-auto px-4
- Отступы между секциями: space-y-6, space-y-8

### 5.5 Анимации и переходы
- Transition-colors для hover эффектов
- Transition-opacity для появления элементов
- Smooth scroll для страницы

### 5.6 Адаптивность
- Mobile-first подход
- Responsive контейнеры
- Адаптивные таблицы (возможна горизонтальная прокрутка)
- Гибкие сетки с flex-wrap

---

## 6. API требования

### 6.1 Базовый URL
- Development: `http://localhost:8080`
- Production: (настраивается)

### 6.2 Аутентификация

#### 6.2.1 Вход
```
POST /auth/login
Body: {
  "username": string,
  "password": string
}
Response: {
  "token": string,
  "player": Player
}
```

#### 6.2.2 Регистрация
```
POST /auth/registrer
Body: {
  "username": string,
  "password": string
}
Response: {
  "token": string,
  "player": Player
}
```

### 6.3 Игроки

#### 6.3.1 Список игроков
```
GET /players/
Response: Player[]
```

#### 6.3.2 Данные игрока
```
GET /players/{id}
Headers: Authorization: Bearer {token}
Response: Player
```

#### 6.3.3 Обновление профиля
```
PATCH /players/{id}
Headers: Authorization: Bearer {token}
Body: {
  "Username"?: string,
  "Description"?: string,
  "Img"?: string
}
Response: Player
```

#### 6.3.4 Лидерборд
```
GET /players/leaderboard
Response: LeaderboardRow[]
```

#### 6.3.5 Игры игрока
```
GET /players/{id}/games-played
Response: GamePlayed[]
```

#### 6.3.6 Обновление записи об игре
```
PATCH /players/{id}/games-played/{game-id}
Headers: Authorization: Bearer {token}
Body: {
  "status"?: string,
  "comment"?: string,
  "rating"?: string,
  "time_played"?: string,
  "start_date"?: string
}
Response: GamePlayed
```

### 6.4 Игры

#### 6.4.1 Список игр
```
GET /games/
Response: Game[]
```

#### 6.4.2 Данные игры
```
GET /games/{id}
Response: Game
```

### 6.5 Типы данных

#### Player
```typescript
{
  ID: string;
  Username: string;
  Description: string;
  Img: string;
  Email?: string;
}
```

#### LeaderboardRow
```typescript
{
  Player_id: string;
  total: number;
  comleted: number;
  drop: number;
  reroll: number;
}
```

#### Game
```typescript
{
  ID: string;
  Points: number;
  HoursToBeat: number;
  Title: string;
  URL: string;
  CreatedAt: string;
}
```

#### GamePlayed
```typescript
{
  Player_id: string;
  game_id: string;
  status: string; // "В процессе" | "Пройдено" | "Дроп" | "Реролл"
  scores: number;
  start_date: string;
  end_date: string | null;
  comment: string;
  rating: string;
  time_played?: string;
}
```

#### AuthResponse
```typescript
{
  token?: string;
  player?: Player;
}
```

---

## 7. Структура страниц

### 7.1 Главная страница (`/`)

**Компонент:** `routes/page.svelte`

**Props:**
```typescript
{
  leaderboard: LeaderboardRow[];
  players: Player[];
}
```

**Структура:**
1. Заголовок
2. Компонент Chart (условно, если есть данные)
3. Таблица лидеров

**Загрузка данных:**
- Функция `loadHomeData()` в `App.svelte`
- Запросы:
  1. `getLeaderboard()`
  2. `getPlayers()`
  3. Для каждого игрока: `getPlayerGames()` и `getGame()` для текущей игры

### 7.2 Страница входа (`/login`)

**Компонент:** `routes/login/page.svelte`

**Состояние:**
- `username: string`
- `password: string`
- `isRegister: boolean`
- `loading: boolean`
- `error: string`

**Функционал:**
- Переключение режима входа/регистрации
- Валидация формы
- Отправка данных через `login()` или `register()`
- Обработка ошибок
- Перенаправление после успеха

### 7.3 Страница профиля (`/users/{id}`)

**Компонент:** `routes/users/[id]/page.svelte`

**Props:**
```typescript
{
  player: Player;
  games: (GamePlayed & { gameTitle?: string; gameURL?: string })[];
  isOwner: boolean;
}
```

**Структура:**
1. Блок профиля
2. Кнопка создания записи (только для владельца)
3. Форма создания записи (модальное окно)
4. Модальное окно редактирования профиля
5. Таблица игр

**Загрузка данных:**
- Функция `loadUserData(id)` в `App.svelte`
- Запросы:
  1. `getPlayer(id)`
  2. `getPlayerGames(id)`
  3. Для каждой игры: `getGame(game_id)` для названия и URL

### 7.4 Layout

**Компонент:** `routes/layout.svelte`

**Структура:**
1. Навигационная панель
2. Слот для контента страниц

**Функционал:**
- Загрузка пользователя из localStorage
- Отображение состояния авторизации
- Функция выхода

---

## 8. Компоненты

### 8.1 Chart.svelte

**Назначение:** Визуализация статистики

**Props:**
- `data: LeaderboardRow[]`
- `players: Player[]`

**Функционал:**
- Переключение периода (неделя/месяц)
- Переключение типа данных
- Столбчатая диаграмма
- Показ топ-10 игроков

### 8.2 Modal.svelte

**Назначение:** Универсальное модальное окно

**Props:**
- `isOpen: boolean`
- `title: string` (опционально)

**Функционал:**
- Backdrop с blur
- Закрытие по клику вне окна
- Закрытие по Escape
- Кнопка закрытия

### 8.3 GameForms.svelte

**Назначение:** Форма создания записи об игре

**Props:**
- `playerId: string`
- `isOpen: boolean`

**Функционал:**
- Поиск игры
- Выбор статуса
- Поля: время, комментарий, оценка
- Валидация
- Отправка данных

---

## 9. Обработка ошибок

### 9.1 Ошибки API
- Отображение сообщений об ошибках
- Fallback значения при ошибках загрузки
- Логирование в консоль

### 9.2 Ошибки валидации
- Отображение под полями формы
- Блокировка отправки при ошибках

### 9.3 Ошибки навигации
- Редирект на главную при 404
- Обработка неверных ID пользователей

### 9.4 Состояния загрузки
- Индикаторы загрузки
- Блокировка интерактивных элементов
- Отображение пустых состояний

---

## 10. Критерии приемки

### 10.1 Функциональные критерии

#### Авторизация
- [ ] Пользователь может зарегистрироваться
- [ ] Пользователь может войти в систему
- [ ] Токен сохраняется в localStorage
- [ ] Пользователь может выйти из системы
- [ ] Состояние авторизации сохраняется при перезагрузке

#### Главная страница
- [ ] Отображается таблица лидеров
- [ ] Отображаются графики статистики
- [ ] Можно переключать период (неделя/месяц)
- [ ] Можно переключать тип данных
- [ ] Ссылки на профили работают
- [ ] Отображается текущая игра для каждого игрока

#### Страница профиля
- [ ] Отображается информация о пользователе
- [ ] Отображается статистика
- [ ] Отображается таблица игр
- [ ] Владелец может редактировать профиль
- [ ] Владелец может создавать записи об играх
- [ ] Игры сортируются по дате (новые сверху)
- [ ] Ссылки на игры открываются в новой вкладке

#### Навигация
- [ ] Работает SPA роутинг
- [ ] Кнопки назад/вперед работают
- [ ] Навигационная панель отображается на всех страницах
- [ ] Состояние авторизации отображается корректно

### 10.2 Технические критерии

#### Код
- [ ] Используется TypeScript
- [ ] Все типы определены
- [ ] Нет ошибок компиляции
- [ ] Код следует структуре проекта

#### Стилизация
- [ ] Используется Skeleton UI
- [ ] Применена тема `wintry`
- [ ] Все компоненты используют Skeleton классы
- [ ] Адаптивный дизайн работает

#### Производительность
- [ ] Данные кэшируются
- [ ] Нет лишних перезагрузок
- [ ] Индикаторы загрузки отображаются

### 10.3 UX критерии

#### Визуальная обратная связь
- [ ] Индикаторы загрузки
- [ ] Hover эффекты
- [ ] Состояния ошибок
- [ ] Пустые состояния

#### Доступность
- [ ] Семантический HTML
- [ ] Правильные атрибуты (id, for, aria-label)
- [ ] Клавиатурная навигация

#### Адаптивность
- [ ] Работает на мобильных устройствах
- [ ] Работает на планшетах
- [ ] Работает на десктопе

---

## 11. Дополнительные требования

### 11.1 Моки для разработки
- Реализованы моки для всех endpoints
- Возможность переключения между моками и реальным API
- Реалистичные данные для тестирования

### 11.2 Обработка edge cases
- Пустые данные
- Отсутствие текущей игры
- Отсутствие аватара
- Очень длинные тексты
- Неверные ID

### 11.3 Безопасность
- JWT токены в localStorage
- Валидация на клиенте
- Защита от XSS (Svelte автоматически экранирует)

---

## 12. Этапы разработки

### Этап 1: Базовая структура ✅
- Настройка проекта
- Конфигурация сборки
- Базовая структура файлов

### Этап 2: Дизайн система ✅
- Интеграция Skeleton UI
- Настройка темы
- Базовые компоненты

### Этап 3: Роутинг ✅
- Реализация SPA роутинга
- Навигационная панель
- Базовые страницы

### Этап 4: API интеграция ✅
- API клиент
- Моки данных
- Обработка ошибок

### Этап 5: Страницы
- Главная страница (Leaderboard)
- Страница входа/регистрации
- Страница профиля

### Этап 6: Компоненты
- Chart компонент
- Modal компонент
- GameForms компонент

### Этап 7: Финальная полировка
- Обработка edge cases
- Оптимизация
- Тестирование

---

## 13. Примечания

### 13.1 Особенности реализации
- Используется кастомный роутинг вместо SvelteKit для простоты
- Моки включены по умолчанию для разработки
- Данные кэшируются на уровне компонента App

### 13.2 Будущие улучшения
- Lazy loading компонентов
- Анимации переходов
- Кэширование с TTL
- Обработка 404 страниц
- Пагинация для больших списков

---

**Версия документа:** 1.0  
**Дата создания:** 2025-01-23  
**Статус:** В разработке

