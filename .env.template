# DATABASE
DB_USER=
DB_PASSWORD=
DB_NAME=playtrack
DB_HOST=postgres-db
DB_PORT=5432
DB_URL=postgresql://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_NAME}

# API
ENV_MODE=production
SERVER_HOST=0.0.0.0
SERVER_PORT=5000
JWT_TOKEN_SECRET=

# FRONTEND
FRONT_NODE_ENV=production
FRONT_HOST=0.0.0.0
FRONT_PORT=3000
